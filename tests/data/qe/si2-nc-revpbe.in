! revPBE on a PBE dataset, requested through input_dft.
!
! Two things at once, both deliberate. The functional is revPBE, which differs
! from PBE only in the exchange enhancement's kappa, so it checks that the
! constant is threaded to the right place and not that the machinery works.
! And it is requested through input_dft, overriding what the pseudopotential
! header says -- the path QE's own pw_dft tests take, and the one that has to
! warn rather than fail.
 &control
    calculation = 'scf'
 /
 &system
    ibrav=2, celldm(1)=10.20, nat=2, ntyp=1,
    ecutwfc=20.0
    input_dft='revpbe'
 /
 &electrons
    conv_thr = 1.0d-10
 /
ATOMIC_SPECIES
 Si  28.086  Si.pbe-rrkj.UPF
ATOMIC_POSITIONS (alat)
 Si 0.00 0.00 0.00
 Si 0.25 0.25 0.25
K_POINTS
  2
   0.250000  0.250000  0.250000   1.00
   0.250000  0.250000  0.750000   3.00
